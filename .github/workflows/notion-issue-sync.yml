name: Notion Issue Sync

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Log the planned sync without writing to GitHub or Notion"
        required: false
        type: boolean
        default: false
  schedule:
    - cron: "*/5 * * * *"
  issues:
    types:
      - opened
      - edited
      - closed
      - reopened
      - assigned
      - unassigned
      - labeled
      - unlabeled
      - deleted
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - edited
      - ready_for_review
      - converted_to_draft
      - closed
      - labeled
      - unlabeled
  repository_dispatch:
    types:
      - notion-issue-sync

permissions:
  contents: read
  issues: write
  pull-requests: read
  repository-projects: write

concurrency:
  group: notion-issue-sync
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22.23.1"

      - name: Verify issue and PR lifecycle contracts
        run: |
          node tests/notion-issue-sync-hotfix-smoke.js
          node tests/notion-issue-sync-pr-lifecycle-smoke.js

      - name: Sync GitHub Project issues and Notion board
        uses: actions/github-script@v9
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
          GITHUB_PROJECT_OWNER: JUNGLE-TEAM1
          GITHUB_PROJECT_NUMBER: "4"
          GITHUB_PROJECT_STATUS_FIELD: Status
          GITHUB_PROJECT_STATUS_ALIASES: '{"In Progress":"In progress","Blocked":"blocked","Review":"In review","Preview":"Ready"}'
          NOTION_PROJECT_STATUS_PROPERTY: Project Status
          DRY_RUN: ${{ github.event.inputs.dry_run || 'false' }}
        with:
          github-token: ${{ secrets.ISSUE_SYNC_PAT || github.token }}
          script: |
            const sync = require(`${process.env.GITHUB_WORKSPACE}/.github/scripts/notion-issue-sync.js`);
            await sync({ github, context, core });
