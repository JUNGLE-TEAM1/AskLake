# Copy this file outside the repository, replace the placeholder, and mount it
# as the root-only source file. The container entrypoint copies it into tmpfs
# at /run/secrets/asklake-clickhouse-v2.properties with uid/gid 1000:1000 and
# mode 0400 before exec-dropping the worker to appuser.
clickhouse.ingest.password=replace-with-distinct-clickhouse-v2-ingest-password
